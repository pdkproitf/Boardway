<%= simple_form_for @play do |f|%>
  <%= f.select :category_id, options_for_select(@categories) , prompt: "Select Category " %>
  <%= f.input :title, label: "Play Title" %>
  <%= f.input :description %>
  <%= f.input :director %>
  <%= f.submit %>
<% end %>
